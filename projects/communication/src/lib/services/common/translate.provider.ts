import { Provider } from '../common/provider';
import { ITranslateDescription } from '../../models/translate-description';

export abstract class TranslateProvider extends Provider<ITranslateDescription> {
}
