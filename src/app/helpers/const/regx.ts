
export const regxImage: RegExp = /(png|jpg|jpeg|svg)$/;

export const regxVideo: RegExp = /(mp4|webm|ogg)$/;

export const regxFile: RegExp = /(docx|doc|excel|csv|indd|pdf|txt|pptx|xls|xlsx)$/;

export const regxHttp: RegExp = /^http[s]?:\/\//;

export const regxLink: RegExp = /(https?:\/\/(?:www\.|(?!www))[^\s\.]+\.[^\s]{2,}|www\.[^\s]+\.[^\s]{2,})/gi;

// TODO: Rename
export const acceptFile = ['.png', '.jpg', '.jpeg', '.ico', '.gif', '.bmp'];

