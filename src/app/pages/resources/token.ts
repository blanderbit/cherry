import { InjectionToken } from '@angular/core';
import { IResourceFormConfig } from './resources.sub-module';

export const resourceFormConfig = new InjectionToken<IResourceFormConfig>('resourceFormConfig');
